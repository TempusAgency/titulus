# Backups made before manual edits (e.g. *.bak-2026-09-16) — git history is the real backup now.
*.bak-*

# Logs (e.g. worker.log from the background summarizer) and local runtime state.
*.log

# OS / editor cruft.
.DS_Store
*.swp

# Internal working docs — local-only, deliberately excluded from the public
# repo (agency-internal methodology, coordination notes, prior client refs).
# Kept on disk for local work; never tracked or published.
/CLAUDE.md
/docs/AUDIT-2026-06-03.md
/docs/AUDIT-UNIVERSALITY-2026-06-03.md
/docs/OPTIMIZATION-HANDOFF-2026-06-10.md
